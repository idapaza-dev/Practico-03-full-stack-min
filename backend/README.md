# Backend (ultra-minimal)
Debes implementar TODO desde cero.

## Pasos mínimos
1. `npm init -y`
2. `npm i express cors express-validator dotenv mongoose`
3. Crear `index.js` (ya incluido con TODOs) y completar los endpoints.
4. Crear `.env` con `MONGO_URI=mongodb://localhost:27017/des421`
5. Ejecutar con `node index.js` o instalar `nodemon` si prefieres modo dev.
